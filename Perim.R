PerimetroCuadrado <- function(L){
  return(4*L)
}

LongitudCircunferencia <- function(R){
  return(2*3.14159*R)
}

# Función para calcular el perímetro de un rectángulo
calcular_perimetro <- function(longitud, ancho) {
  perimetro <- 2 * (longitud + ancho)
  return(perimetro)
}